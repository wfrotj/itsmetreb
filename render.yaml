services:
  # One Render service: build frontend, then serve site + contact API
  - type: web
    name: itsmetreb
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm start
